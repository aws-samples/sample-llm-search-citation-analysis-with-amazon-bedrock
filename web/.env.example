# API Configuration
# For local development, you can override the API URL
# VITE_API_URL=https://your-api-gateway-url.execute-api.us-west-2.amazonaws.com/prod/api

# The default API URL is already configured in src/config.ts
# Only set this if you want to use a different API endpoint
